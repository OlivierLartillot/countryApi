{% extends 'base.html.twig' %}

{% block title %}EndpointCategory{% endblock %}

{% block body %}
    <h1>EndpointCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ endpoint_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ endpoint_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_endpoint_category_index') }}">back to list</a>

    <a href="{{ path('app_back_endpoint_category_edit', {'id': endpoint_category.id}) }}">edit</a>

    {{ include('back/endpoint_category/_delete_form.html.twig') }}
{% endblock %}
