{% extends 'base.html.twig' %}

{% block title %}Department{% endblock %}

{% block body %}
    <h1>Department</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ department.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ department.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ department.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_department_index') }}">back to list</a>

    <a href="{{ path('back_department_edit', {'id': department.id}) }}">edit</a>

    {{ include('back/department/_delete_form.html.twig') }}
{% endblock %}
