<form method="post" action="{{ path('app_back_endpoint_delete', {'id': endpoint.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ endpoint.id) }}">
    <button class="btn">Delete</button>
</form>
