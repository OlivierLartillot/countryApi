{% extends 'base.html.twig' %}

{% block title %}Endpoint{% endblock %}

{% block body %}
    <h1>Endpoint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ endpoint.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ endpoint.description }}</td>
            </tr>
            <tr>
                <th>Endpoint</th>
                <td>{{ endpoint.endpoint }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_endpoint_index') }}">back to list</a>

    <a href="{{ path('app_back_endpoint_edit', {'id': endpoint.id}) }}">edit</a>

    {{ include('back/endpoint/_delete_form.html.twig') }}
{% endblock %}
